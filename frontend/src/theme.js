import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#e2cc79",
            light: "#f2de6e"
        },
        secondary: {
            main: "#f2de6e"
        },
        otherColor: {
            main: "gray"
        }
    }
})